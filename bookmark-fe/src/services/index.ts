// This file can be used for utility functions or non-RTK Query API helpers
// RTK Query API logic is now in /stores/slices/bookmarksApi.ts

// Example utility function for external APIs or non-bookmark related services
export const exampleUtility = async () => {
  // This would be for external APIs or utility functions
  // that don't need the full RTK Query caching/state management
}

// You can add other utility functions here like:
// - URL validation helpers
// - Data transformation utilities
// - External API integrations (analytics, etc.)
// - File upload services
// etc.
